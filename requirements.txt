# logic-app-toolkit requirements
# Single bundle — everyone who bootstraps can run tests immediately.
# Pinned versions, vendored as wheels for air-gapped install.
pyyaml==6.0.3
textual==3.1.0
rich==13.9.4
pytest==8.3.4
